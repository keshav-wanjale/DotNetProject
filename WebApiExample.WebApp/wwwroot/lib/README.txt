This folder is organized according to ASP.NET Core conventions for third-party libraries.

Each library should be placed in its own subfolder:
- /lib/jquery/
- /lib/bootstrap/
- etc.

This structure replaces the previous .NET Framework organization where libraries were stored in:
- /Scripts/ (for JavaScript files)
- /Content/ (for CSS files)
- /fonts/ (for font files)